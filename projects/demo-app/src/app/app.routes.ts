import { Routes } from '@angular/router';
import { DemoFilterComponent } from './demo-filter.component';

export const routes: Routes = [
  { path: '', component: DemoFilterComponent },
  // Add more demo routes here if you like
];
